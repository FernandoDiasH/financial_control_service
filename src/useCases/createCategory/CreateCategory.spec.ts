import {describe, it, } from 'vitest'
import { createCategoryController, createCategoryUseCase } from '.'

describe('Quando eu registar uma categoria nova', ()=>{
    it('que ela registre', ()=>{

        // createCategoryUseCase.execute({
        //     user_id:1, 
        //     descripiton:"teste" }
        // )
    })
})